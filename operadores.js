let a=1, b=2;
const nome = "anottsu";
console.log(a === b); // COMPARA A COM B O RETORNO É FALSE.
console.log(`O nome ${nome.replace(/\b\w/g, (nome)=> nome.toUpperCase())} possui ${nome.length} caracteres`);
